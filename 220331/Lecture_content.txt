 컴파일러에 따라 바이트가달라짐. 
자료형 double이 무조건 8바이트로 확정된것은아님

ㅁ 배열 선언 시 주의점
 ㅇ 중괄호로 초기화
배열의 선언 및 초기화는, 중괄호를 쓸시에는 무조건 동시에.
아니면 배열마다 따로따로 초기화 해야함.

 - const 상수는 배열크기로 사용 불가. 
 	값이아니라  변수이기때문. 

이차원배열 선언시 배열크기값중 열의 값은 반드시 선언필요
?배열을 함수인자로보냈을땐 배열의크기를알수가없다.
배열이아닌 주소값만 있기때문













